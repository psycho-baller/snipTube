import { type FC } from 'react';

interface Props {

}

const Home: FC<Props> = (props) => {
  const { } = props;

  return (
    <div className="">
      Home
    </div>
  );
};

export default Home;
